# requirements.lock — 已弃用（2026-09-01 起）
#
# 已按目标环境拆分为两份，请勿继续使用本文件：
#   - requirements.lock.cpu  → CI / CPU 环境（pip install -r requirements.lock.cpu）
#   - requirements.lock.gpu  → GPU / CUDA 环境（pip install -r requirements.lock.gpu --extra-index-url https://download.pytorch.org/whl/cu121）
#
# 历史内容（GPU 基准环境，2026-05-24 验证）保留于 requirements.lock.gpu。
