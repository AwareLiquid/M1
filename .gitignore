# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environments
.env
.venv
env/
venv/
ENV/

# Training artifacts
checkpoints/
runs/
wandb/
*.pt
*.bin
*.safetensors

# Experiment output dir (grid-cell self-clones, figures, logs).
# Ignored wholesale to avoid committing multi-MB run output and the
# self-clone working copies (one of which used to leave a recursive
# symlink artifacts/gridcell_local/M1 -> repo root that broke full-repo
# pytest collection). The handful of committed trace/needle fixtures
# below are explicitly kept tracked.
artifacts/
!artifacts/needle_chat_qwen05b.json
!artifacts/real_trace_demo.jsonl
!artifacts/real_trace_demo_audit.json
!artifacts/real_trace_v2_test.jsonl
!artifacts/real_trace_v3_test.jsonl

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Datasets cache
~/.cache/huggingface/

# One-off / throwaway scripts (deck editors etc.) kept locally, not tracked
scripts/oneoff/

# Kaggle build/run output dirs
_kaggle_out/
kaggle_out/
kaggle/_sft_out2/

# API / SFT scratch output dirs (machine-generated, not source)
_apiout/

# Benchmark run outputs (JSON results are regenerated; keep the .py harness)
benchmarks/physics_out/
benchmarks/scaling_out/
benchmarks/recall_out/

# Training run output dirs (checkpoints, logs â€” large, not source)
m2_out_v7/
m2_out_*/

# Experiment run logs (keep .py/.json/.md reports; ignore raw run logs)
experiments/_*.log
experiments/_*.txt

# Duplicate scratch copy of kaggle kernels (source lives in kaggle_kernels/)
kaggle_kernels_ea1/

# Stray build/verification logs at repo root
/checkpoint_wrapup.log
/docker_verification.log

# Bundled fonts
NotoSC*.otf

# Local agent / tool state and binaries
.claude/
.tools/

# Nested external clone (its own git repo), kept out of this tree
temp_nature_skills/

# SSH / TLS private keys and secrets -- NEVER commit these
*.pem
*.key
*.ppk
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*_rsa
*_ed25519
*.crt
secrets/
*.secret

# Local experiment scratch (Kaggle/Modal artifacts, diag scripts, run logs)
/_kaggle_*/
/_kgl_*/
/_o1_*/
/_o1_*.py
/_diag_*
/_compare_baselines_rerun.log
/_modal_*.log
/o1-pretrain-48m.log
/M1/
/_kaggle_fetch_final.py

# Benchmark run outputs (JSON/ckpt artifacts land here at runtime)
benchmarks/attribution_out/
benchmarks/recall_out/
benchmarks/length_out/
/_kgl_ckpt_dl.log
/temp_nature_skills
benchmarks/_needle_smoke.json

# Local test run logs + opencode session dir (never source)
/_*.log
/.omo/

# local scaling benchmark run outputs (2026-07-16)
/scaling_fp32/

# ONNX feasibility scratch output
benchmarks/_onnx_feasibility.onnx
benchmarks/_legacy.onnx
# NASA battery dataset (209 MB, download via the URL in battery_soh_edge.py)
/data/nasa_battery/

# partner referral runtime counts (not source)
/data/partners/

# Deployment access notes â€” never committed (public repo)
*.local.md

# Kaggle credentials (KGAT_ tokens ¡ª never commit)
/.kaggle/

# text A/B checkpoints (large, reproducible from script)
/data/text_ab_ckpt/

# Web assets repo (keep separate)
AwareLiquid-Web/

# Experiment run logs (E1/E2/E5 parity/length-extrapolation series)
/logs/
