# Parametric memory engine v0 + four-competency bench

## What

把本仓库已证明的"快权重联想记忆"（cross-window recall 0.56 锚点、snapshot 位精确）
封装成 **Mem0/Letta 接口形态的参数化记忆 runtime**，并建立 MemoryAgentBench
(arXiv:2507.05257) 四能力坐标系下的评测基准。

- `mt_lnn/parametric_memory.py` — `ParametricMemory`：write / recall / forget /
  snapshot / state_bytes；`update_rule="sum"|"delta"` 双写规则开关（对齐
  mt_v2 / mt_v2_delta 两种已训练机制）；forget 为 delta 投影
  `F ← F − k(kᵀF)`，无绑定索引、O(1) 状态下单绑定抹除；快照经
  `session_state.py` 的 `HFSessionState.fw_state`（新增向后兼容字段）原子落盘，
  base64 位精确往返
- `benchmarks/parametric_memory_bench.py` — 四维基准：D1 accurate retrieval
  （in-window）/ D2 test-time learning（drop-KV 跨窗，0.56 锚点口径：75% cross
  mix、lr 1e-3、state_scale_init 0.1）/ D3 conflict resolution（同键二次写入须答
  最新值；sum 叠加 vs delta 纠错，如实报）/ D4 cross-session long-range（快照
  →磁盘→**真子进程**恢复→召回，含 no-restore 对照）。对照配置
  baseline/lora_only（结构性零）/ mt_v2（sum）/ mt_v2_delta（delta）+ 零依赖
  BM25 外挂库对照（精度/latency/存储字节随条目增长曲线）+ ParametricMemory
  runtime 的 O(1) 状态字节与 sqrt(d/N) 容量曲线
- `tests/test_parametric_memory.py` — 10 项单测：写入→召回（token-id 与文本
  路径）、快照位精确、forget 隔离性（目标绑定 ≤ chance、其余存活）、状态字节
  恒定、delta-vs-sum 冲突机制；CPU <5s
- `docs/PARAMETRIC_MEMORY.md` — 定位页：与 Mem0/Letta/Titans 机制对比表 +
  迭代日志（目标、关键决策、修复清单）
- `HANDOFF.md` §8 — 执行手册：全量跑法（分阶段 A/B/C）、判读红线、入库顺序

## Status（诚实声明）

**代码侧完成、全量实验待跑。** 本 PR 不含任何实验数字 —— 四维结果须由
`benchmarks/parametric_memory_bench.py` 全量（4 配置 × 3 seeds × 3000 步，
~3.5h MPS）产出 JSON 后，按 HANDOFF §8.3 入库 BENCHMARKS/RESULTS/README，
届时补进 PR 描述。`--smoke` 已在 CPU 端到端验证（含 D4 子进程路径）。

协议纪律：每配置 ≥3 seeds、per-seed + mean±std、JSON 落
`benchmarks/results/parametric_memory_*.json`、resume-safe；单 seed 结论不可
引用；不引用任何 retracted 主张；零新增第三方依赖。

## 机制一句话

外挂库（Mem0/Letta/Zep）：记忆 = 向量库/文件/明文日志，存储 O(n)、无法位精确
恢复、选择性删除靠删日志。本 PR：**记忆 = 快权重矩阵 F 本身** —— 每会话
(d²+d) 浮点数恒定、快照位精确、forget 是代数不是删行；代价是固定状态宽度下
容量有限（~sqrt(d/N) 读信噪比边界，bench 实测曲线如实报）。

## 与并行 PR 的关系

irregular-streaming-edge / o-series-hybrid-ratio 等线与本分支零文件交叉；
唯一潜在交叠是 HANDOFF.md 追加节（合并时两节都保留）。
