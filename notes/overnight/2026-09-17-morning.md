# 晨报 2026-09-17（B-2' staircase 校准日）

> 继承：B-2 线已于昨日关闭（flat/天花板，双硬件互证）；B-2'（难度硬化）
> PREREGISTERED 于 overnight/loop（commit 4ed8941，staircase 二分校准法）。

## 轮次（10:4x）：B-2' staircase 校准 kernel 推送（T2 Kaggle T4 首用）

- **kernel**：`aricredemption/m1-tau-staircase-b-2-calibration`（T4，bundle
  已挂载）——扫描 k ∈ {32,64,128} × depth ∈ {1,4}（ladder off+on，seed 0，
  20K 步 + 10K gate 读数）= 12 配置。**进度日志约定已内置**：[cfg i/N]
  START/DONE/wall + [eta] 均值外推 + [summary] k/d 一览表 + 探针自带
  step/loss 每 2500 步。**按 k 分目录防覆盖**（发现并修复：探针输出文件名
  不含 difficulty，同目录会互相覆盖 + resume 误跳）。
- **状态**：RUNNING（10:53 起，已过 4 秒崩溃窗口 → dataset 挂载生效）。
  T2 配额首用（30h/周，预计消耗 ~1h）。
- **slug 备注**：实际 slug 由 title 派生为 `...-b-2-calibration`（metadata
  已修正，后续 push/status/output 用此 slug）。
- **判读（完成后）**：找 d1 gate<0.55 而 d4 gate≥0.55 的 k 区间 = 深度
  敏感带 → 带内 18 配置全矩阵（A100 通道，判据同款 judge_verdict）。

## 轮次（11:1x）：估算失准问题的核实与制度化（用户质询）

- **对账**：V14（估 6 配置/实 0——metadata 缺挂载，已修，V16 实跑 7 配置
  证明修复）；V16（估 8.8/实 7——d2 配置 7.5h 方差，接受并加余量）；
  staircase（估 T4 ~1h——**推送前无实测，正是问题模式**）。
- **核实**：网页 Logs 标签的活跃计时器（Running for 663s→推进中）= 可靠
  存活判据；script kernel 的逐步日志只在会话结束后可拉（API 与编辑器
  均不给运行中内容）→ **运行中无法逐步监控是平台限制**。
- **制度化（三步）**：① L011 存活判据改用网页活跃计时器（status 端点
  不可信）；② kernel 自带 [eta] 行（首配置完成即自校准总时长）✓ 已内置；
  ③ 今后新 kernel 的时长估算一律标注"待首配置实测校准"，首个 wall 出来
  后在晨报修正。
- **staircase 修正预期**：12 配置（每配置 = 2 ladder × 20K 步，T4）。
  完成时点即可从日志读出首配置 wall 校准全部预估。

## 轮次（11:3x）：staircase 实时进展 — T4 速率实测 + 难度硬化生效

- **ego-browser 实读 Logs 标签**（修正 L011：运行中 kernel 的 Logs 标签
  **会流式显示日志**——此前 663s 时未刷出内容是渲染时机问题，非平台限制；
  存活判据用计时器 + 日志流双确认）。
- **T4 速率实测**：~31 steps/s（31ms/步）——落入预估的"中情景"。单配置
  20K 步 ≈ 10.8min，6 个 (k,d) 对总时长 ≈ **2.2h（预计 ~14:30 完成）**，
  9h 窗余量充足。
- **进展**：cfg 1/6（k=32 d=1）——ladderoff 10K 步完（loss 2.59→0.69），
  ladderon 进行中。**校准信号：k=32 的 loss 卡在 ln(2)≈0.69 = chance**
  ——k=16 全饱和、k=32 即 chance，**难度硬化生效，深度敏感带大概率在
  k=32 附近**。cfg 2/6（k=32 d=4）的 d4 gate 表现是下一个关键读数。

## 收割轮（21:5x）：B-2' staircase 校准完成 — k=32 出现 H004 预言形状

- kernel COMPLETE（344min，16:34 结束）→ 收割 15 文件入
  `benchmarks/results/tau_staircase/`（32 files 含重复合并目录）。
- **核心读数（seed 0，深度增益 d4-d1）**：
  | k | ladder off | ladder ON |
  |---|---|---|
  | 32 | -0.024 | **+0.054** |
  | 64 | +0.025 | -0.013 |
  | 128 | -0.235 | -0.225（d4 双臂撞墙 gate=0.5） |
  **k=32 处 τ 阶梯把深度效应从负转正——H004 预言的形状首次出现**（单
  seed，待 3-seed 显著性）。k=128 d4 双臂撞墙 = 高难度下深度本身有害
  （ladder 无关，另一条机制线索）。
- **带内定标：k=32**。下一步 = 预注册完成步：18 配置（2 ladder × 3 depths
  × 3 seeds）× 30K 步 @ k=32（A100 通道，judge_verdict 2σ+符号检验）→
  H004 正式判定。commit `34945ce`。
