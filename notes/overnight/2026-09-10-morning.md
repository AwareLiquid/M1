# 晨报 2026-09-10（白班增量：B-1 Kaggle 通道收尾）

## B-1 终态确认与 Kaggle CPU kernel 处置

- **B-1 已终态 DEAD**（backlog c9112cc，everest-an 服务器 A100 通道执行）：
  selective 2/3 配对胜但 sign-test p=0.5 不显著，8K→32K 增益未放大。
  本文件不重复判定，仅登记 Kaggle 侧遗留物处置。
- **Kaggle CPU kernel `aricredemption/m1-b1-text-selective-ab-32k`（V8）建议取消**：
  - 今晨 10:11 +0800 起 Save & Run All（版本历史 V1–V7 均为配置调试失败，V8 为首个成功运行的版本）
  - 实测 2.29–2.33 s/step（4 vCPU 满负荷，torch 2.10.0+cpu threads=4；M2 Max 微基准
    1线程 0.882 s/step / 4线程 0.452 s/step，加速比 1.95×，负载仅能吃约 2 核当量）
  - 单组合 32K ≈ 20.7h，6 组合 ≈ 124h >> 12h 会话上限；22:11 +0800 强杀时约
    step 18.8k（组合 1 的 59%），jsonl 不落盘（结果仅训练结束后写）。
    B-1 判定已由 A100 通道完成，V8 结果冗余。
  - 处置结果：**用户拍板取消，2026-09-10 16:33 +0800 经网页端 Cancel 执行，
    API 确认 CANCEL_ACKNOWLEDGED**。中断时约 8h45m / step ~13300（组合 1 的 42%），
    mid-ckpt @2000/4000/6000/…/12000 随失败版本 Output 保留（B-1 已 DEAD，
    这些 ckpt 暂无认领方，如需 CPU/GPU 复现对照可从 V8 版本 Output 取回）。
- **脚本日志增补（d8137db）**：benchmarks/text_selective_ab.py 步进日志现含
  combo 序号、步数百分比、s/step、ETA，组合级打印会话耗时与 12h 预算预警。
  已 py_compile 通过；对 Kaggle 生效需随下次 bundle dataset 更新一并推送。
- **监控自动化**：B-1 watchdog（每 30 min，共 9 轮）随 B-1 终态达成已删除。
- 附注：V8 运行期日志经 ego-browser（登录态）抓取验证；Kaggle API 在 RUNNING 期
  间 kernels status 可用但 output/log 为空，实时日志仅网页端可见。

## 需人工判定

1. ~~V8 取消 or 任其跑满 12h~~ → 已裁定并执行（16:33 Cancel）。
2. PR #54（overnight/loop → main）已消解与 main 的冲突（fbcea63，
   --sel/--seed 入口 × combo/ETA 日志共存，py_compile 通过），GitHub 报
   MERGEABLE，待合并进 main。
