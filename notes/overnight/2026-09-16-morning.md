# 晨报 2026-09-16（凌晨轮，02:05-02:2x CST，手动即时触发）

> 运行模式说明：**每 30 分钟定时任务已挂**（automation `automation-c8caa436`，
> 永续循环）——本轮为用户指令的即时首轮，后续由定时任务每半小时自动派发。
> /goal headless 不可用（9-9 已实证）→ 降级自查协议照旧。

## 本轮主产出：B-13' DONE — T2 全系统矩阵收尾

- **方向来源**：backlog 池内 B-13"全矩阵"声明的欠账（L001 纪律：未入库数字
  不可引用）——T2 格缺 micro_fw / micro_rls（9-12 有产物未入库）与 none（从未跑）。
- **预注册先于执行**（commit `970b5ed`）：三门写死——①确定性门（复算与 9-12
  产物逐字节一致）②none 地板门（recall=0.0 ×3）③结构门（9 JSON 全字段）。
- **执行**：9 单任务配置 **8.9s CPU**（--task t2，无 GPU / 无 e5 下载）。
- **验收**：三门全过——确定性 **6/6 字节级一致**（含 microrls s2 stale=0.125
  孤例复现）；none 地板 3/3=0.0；结构 9/9。
- **T2 矩阵终态（3-seed 均值 recall/stale_ans）**：
  | 系统 | recall | stale_ans |
  |---|---|---|
  | param_sum / param_delta / rag(e5) | **1.000** | 0.000 |
  | rag(hash) | 0.917 | 0.042 |
  | **micro_fw（SGD 覆写）** | **0.792** | 0.000 |
  | param(falcon) / micro_rls / none | 0.000 | — |
  | oracle | 0.000 | **1.000**（设计性塌陷） |
- **科学结论（本轮增量，可引用）**：SGD 覆写写在流式更新上 0.792——
  灾难性干扰的 recency 偏置恰是"新值胜出"语义所需的归纳偏置（预注册预期
  被复算精确证实，无叙事降级）；回归式写（RLS/NLMS）归零；叠加式写与
  e5 检索满分。**T2 成为 PMB 首个 9 系统全覆盖任务族。**
- 证据：`benchmarks/results/pmb_t45/pmb_t2_{microfw,microrls,none}_s{0,1,2}.json`
  （9 文件，commit `86964e7`）。

## 卫生清理与状态核查

- **删除 3 个冗余副本**：`pmb_e5/pmb_t2_rag_e5_s{0,1,2}.json`——与 B-9 已入库
  task-all 文件的 t2 格逐值一致（三重核对后清理，未入库过的重复产物）。
- **B-2 tau V14**：`m1-b2-tau-ladder-cpu` **RUNNING**（kaggle CLI 实查，
  9/15 15:04 UTC 起跑）。**收割窗口预估 ~11:04 CST**（12h 上限截断）。
- **GPU 通道**：#53 仍 OPEN + Secret 未配 → 维持阻塞（B-4 / B-12 挂起）。
- **c1_global_heads/**（kaggle_kernels/ 下未跟踪目录）：2026-07-30 旧 era 产物
  （对应 Kaggle 旧 kernel mt-lnn-c1-global-heads），不在当前循环池——**待用户
  裁定归档入库或删除**，本轮未动。

## 本轮 commit（均 push 至 overnight/loop）

- `970b5ed` backlog: B-13' 预注册（三门先于执行）+ B-2 V14 RUNNING 核查
- `86964e7` evidence(pmb): B-13' DONE — T2 矩阵 9 系统收尾 + 冗余清理

## 下一方向（已回池浮现，见 backlog 统计追加）

1. **B-2 V14 收割**（~11:04 CST 后的轮次）：`kaggle kernels output` 拉取已完成
   配置 JSON 入库；parity 域配对齐 → 按预注册判定（acc 全 <0.25 → DEAD /
   learnability gate）。
2. V14 未到收割点的轮次 → 扫描轮浮现新方向（池内本地可执行项已耗尽）。

## 需 AricRedemption 人工判定

- [ ] #53 合并 + Kaggle Secrets `GITHUB_TOKEN`（GPU 关键路径，最高优先）
- [ ] #52 合并（治理护栏）；#58 累积证据 PR 处置
- [ ] c1_global_heads/ 旧 era 目录：归档入库 or 删除

---

## 扫描轮 3 增量（02:4x，用户指令即时触发）：前沿整合评估

**结论先行**：扫出 1 个高价值整合候选（Kalman Delta 写规则，9 月新论文）、
1 个差异化核查（Memora 竞品——PMB 三差异化全部存活）、2 个理论锚
（容量 scaling）、1 个行业定位强化（t4 JOIN = 行业最难开放问题的机械最小化）。
池子由空转补：PREREGISTERED +3（B-14/B-15/B-17）、CANDIDATE +1（B-16）。

| 发现 | 处置 |
|---|---|
| [Kalman Delta Networks](https://arxiv.org/abs/2609.07816)（2609.07816，9 月新）：delta rule 升级为 Kalman innovation 更新，闭式 Möbius 递推 numpy 可实现；只测过序列内 LM，**跨会话从未被测** | **B-15 PREREGISTERED**——`kalman_delta` 写规则入 parametric_memory.py，判负标准写死（难格 ≤ sum+0.05 且 T3 不更平 → DEAD）；其"不确定性随无写入增长"是 T3 遗忘曲线首个机制性预测 |
| [Memora](https://arxiv.org/abs/2604.20006)（2604.20006）：weeks-months 个性化记忆基准，LLM 记忆体 + FAMA 失效感知指标 | B-10 对照更新——无位级持久化、无遗忘曲线 → **PMB 三差异化全部存活**；"频繁复用失效记忆"发现与 PMB t2 stale 指标机械对应 |
| [谱优化器联想记忆容量 scaling](https://arxiv.org/abs/2603.26554) + [Cabannes 标度律](https://arxiv.org/abs/2310.02984) | **B-17 PREREGISTERED**——sum 规则 d-sweep {256,1024,4096}：难格 0.385 是容量瓶颈还是干扰瓶颈，一次探针定夺 |
| [FW Product Key Memory](https://arxiv.org/abs/2601.00671) + [mem0 2026 报告](https://mem0.ai/blog/state-of-ai-agent-memory-2026)（cross-session identity resolution = 行业最难开放问题） | **B-16 CANDIDATE**——t4 JOIN 攻坚（两跳检索/product-key 基线），待读原文写死判负标准 |
| [End-to-End TTT](https://arxiv.org/abs/2512.23675)（Nvidia）、[TTT expressivity](https://arxiv.org/abs/2608.21308)、[TTD 写入门控](https://arxiv.org/html/2603.13875v1) | B-10 对照范围归档，不新开方向；MoNe 仍无公开代码（B-12 维持挂起） |

**执行序**：下轮起 B-14（T3×5 补曲线，18 配置秒级）→ B-17（d-sweep 秒级）→
B-15（kalman_delta 实现，分钟级）；B-2 V14 收割（~11:04 CST）优先插入。
今日扫描配额已用讫（新提示词：池空当日至多一次扫描）。

---

## 定时轮次（02:36 触发）：B-14 DONE — T3 首批参数化遗忘曲线

- **开场**：pull 无新提交；B-2 V14 仍 RUNNING（收割窗口 ~11:04 CST 未到）。
- **执行**：18 配置 92s CPU，结构 18/18、3-seed sd ≤ 0.078、与 B-6 参照吻合。
- **核心发现（预注册"证伪→升格"分支触发）**："叠加写无衰减"被证伪——真相是
  **阶跃式容量断崖**：param_sum/delta 在 16/32/80 条事实全 1.0，累计 272 条
  （≈ 状态维度 d=256）断崖归零；micro_fw 呈唯一单调级进遗忘
  （0.46→0.35→0.04→0.0）；检索式平坦免疫。**三类系统三种曲线形态，T3
  差异化主张的证据链就此补全**。falcon t4 全格 0.0（五任务族全败闭环）。
- 证据：18 JSON 入 `benchmarks/results/pmb_t45/`（commit `d82538b`）。
- **下一方向自然浮现**：B-17 d-sweep（断崖≈d 的预测已锐化：1024/4096 应外推
  断崖 ~4×/16×，判负标准不变）→ B-15 kalman_delta 实现。

---

## 定时轮次（02:59 触发）：B-17 DONE — 容量墙实锤，矩阵叙事修正

- **执行**：新驱动 `dsweep_t1_hard.py`（仅难格 (64,16)，与全网格同评分路径，
  d=256 与已入库 0.385 逐值吻合过确定性门）；d {256,1024,4096} × 3 seeds，
  198s CPU。B-2 V14 仍 RUNNING（kaggle API 本轮 SSL 瞬断一次，未到收割窗不影响）。
- **结果**：难格 recall **0.385 → 0.849 → 0.995**（d=256/1024/4096；
  state 794KB/7.35MB/96.5MB，延迟 1.4/5.0/26ms）。lift +0.609 >> 预注册阈值 +0.05。
- **判定**：**DONE——容量瓶颈确认**。sum 的难格亏损是状态容量墙而非机制失败；
  d=4096 追平检索式（0.995 vs e5 1.0），等召回状态成本 ~42× 于 hash 检索
  （诚实入表）。B-13"参数化 < 检索"排序加容量限定词；B-14 的 T3 断崖获得
  第一性解释（272 条 ≈ d=256）。矩阵结论升级为可引用句（见 backlog B-17）。
- 证据：`benchmarks/results/pmb_dsweep/` 9 JSON + 驱动入库（commit `6193946`）。
- **浮现 B-18（下轮执行）**：容量修复是否跨写规则？同一驱动跑 delta/falcon
  对照臂 × d {256,4096}——若 delta 也被容量救活则"容量是唯一的墙"，
  若只有 sum 受益则叠加式×容量正交，写规则排序叙事再升级。

---

## 定时轮次（03:29 触发）：B-18 DONE — 写规则分类学闭环

- **执行**：delta/falcon_nlms × d {256,4096} × 3 seeds = 12 配置 ~6 分钟；
  确定性门双臂 PASS（d=256 与已入库全网格逐值一致）。B-2 V14 仍 RUNNING。
- **结果**：**delta 0.240 → 0.990**（lift +0.750，容量救活 ✓）；**falcon
  0.000 → 0.005**（对容量免疫，B-7 判负结论经阴性对照稳固）。
- **判定**：**DONE——容量定律跨叠加族推广**。五轮合并的写规则分类学（可引用）：
  叠加（sum）与减法覆写（delta）属容量受限族（d=4096 均恢复至 ~0.99）；
  回归式（NLMS/RLS）属容量不变失败族（任何 d 下 0.0）；跨会话持久化的代价
  是状态容量而非写机制，检索在等召回下保持 ~42× 状态效率优势。
- 证据：`benchmarks/results/pmb_dsweep/` +12 JSON（commit `fbaa77c`）。
- **下一方向**：B-15 kalman_delta 实现（主方向）→ B-19 梯度写容量臂
  （CANDIDATE，分类学最后一块）；B-2 收割 ~11:04 CST。

---

## 定时轮次（03:59 触发）：B-15 DEAD — KDN 首测判负 + 机制诊断

- **实现入库**：`kalman_delta`（isotropic KDN，证据加权 NLMS：增益随累积证据
  收缩、会话边界过程噪声再膨胀）；u 仅 kalman 会话入快照，其余规则快照字节
  不变；test_pmb 9/9 过。
- **结果**（d=256, 3 seeds, 59s）：t1 难格 **0.0**（判负线 0.435）· t2 **0.042**
  · T3 全零 → 预注册双条件同时触发 **DEAD**。
- **机制诊断（本轮真产出，可引用）**：各向同性标量协方差把"同一函数的证据
  累积"套在开放世界绑定流上——写增益 ~1/n 调和塌缩，~10 条事实后写通道关闭。
  **证据加权门控是开放世界记忆的反模式；不确定性必须按键局部化。** KDN 的
  LM 预训练场景（函数固定）与跨会话记忆场景（函数持续扩张）的适配边界由此标定。
- 止损纪律：同方向变体（对角/per-binding u）不重开；实现保留作阴性对照。
- 证据：9 JSON + 实现（commit `27c74d7`）。
- **下一方向**：B-19 梯度写容量臂（分类学最后一块）；B-2 收割 ~11:04 CST。

---

## 定时轮次（04:29 触发）：B-19 DONE — 三族分类学确立

- **执行**：12 配置（micro_fw/micro_rls × d {256,4096} × 3 seeds），~20 分钟
  （d=4096 micro_fw 单配置 3.4 分钟）。B-2 V14 仍 RUNNING。
- **确定性门插曲（门起作用的实例）**：初次对检 FAIL——引用了修复前档案族
  `microfw`（8892f40，[0,0,0]）；核对后确认修复后权威族 `micro_fw`
  （eb01dfc，[0,0.016,0.031]）与新跑逐值一致 → PASS。judge_t45.py 映射已
  修正指向修复后族（原先会输出废止数字）。
- **结果**：micro_fw 难格 0.016 → **0.010**（d 256→4096，state 1.5MB→187MB）
  ——**容量不救梯度写**（阈值 >0.05 未触发）；micro_rls 平坦（快照体积恒
  14KB 不随 d——登记审计小项，其容量结论标不确定、机制性结论不受影响）。
- **判定 DONE：三族分类学（可引用）**——叠加外积写容量受限但**可随状态扩容
  救活**（sum→0.995/delta→0.990）；梯度写（SGD）**机制性失败**（1.5MB 到
  187MB 状态全零区）；回归式（NLMS/RLS）容量不变失败。唯一能骑容量到检索
  平价的是叠加族。
- 证据：+12 JSON（`pmb_dsweep/`，commit `5275d29`）。
- **下一方向**：B-16（读 arXiv:2601.00671 定判负标准 → 两跳检索基线）；
  ~11:04 B-2 收割优先。池内 PREREGISTERED 仅剩外部依赖项（B-2/B-4）——
  若 B-16 读文+实现一轮做不完，按新规下轮收工等收割。

---

## 定时轮次（04:59 触发）：B-16 DONE — 两跳检索修复 JOIN，协议缺口配对论证成立

- **执行**：`RagTwoHopSystem`（hop-1 问题检索 top-3 解析评分→人名 / hop-2 人名
  查询 top-2，hash 编码器同索引）；t4 × 3 seeds + t1 sanity。
- **过程教训（验收自查救场）**：首版正则按文档模板要求 "out of 5"，当前问题
  模板没有——两跳从未触发，首轮 0.083 全是单跳回退（与今日 plain rag 逐格
  一致暴露此问题）。修正后重跑。
- **结果**：t4 难格 (8,4) **0.0 → 0.583**（>0.5 阈值 → DONE）；(8,0) **0.0 →
  1.000**；hop-1 命中率 58%。hash 两跳 ≈ e5 单跳（0.625）。
- **学术结论（可引用）**：JOIN 失败是检索协议缺口而非信息缺口——与 t5b
  拒答阈值配对，**evidence-mode 记忆的两大头条失败模式皆为协议缺口**。
- **档案漂移发现（L 候选）**：今日同跑 plain rag 与 B-8/B-6 档案有 1-2 题/格
  漂移——生成器自 9/9-9/11 演进，旧 JSON 不对当前代码逐位可复现；跨时点
  对照必须同跑（本轮全部判定均同跑对照）。
- 证据：6 JSON + 实现（commit `9caac40`）。
- **下一方向**：B-2 V14 收割（~11:04 CST）优先；次选 mechanism 小项
  （judge 加 rag_2hop 行 / RLS 快照审计）。

---

## 定时轮次（05:29 触发）：mechanism 小项双清

- **judge_t45 加 rag_2hop 行**：复算表首次出现 t4(8,4) 非零（0.583），
  (8,0)=1.0——协议修复后的 JOIN 格首次进入常驻可复算视图。
- **RLS 快照审计结案（无 bug，反向升级）**：micro_rls 快照存完整 pairs 列表
  （键值字符串对），restore 确定性重解 W——14KB 恒定体积是按设计（pairs 为
  字符串，与 d 无关），非漏存。**B-19 的"RLS 容量结论不确定"解除并升级**：
  n=272 ≪ d=4096 精确求解域（零干扰理论上限）下 recall 仍 ~0.016 →
  RLS 失败与存储容量彻底无关，机制在读出侧（归因开放，未立案）。
  micro_fw 快照同批审计：W/codes/vals 入档，P 投影种子确定性再生成（功能
  等价）。commit `19fa066`。
- **池子终态**：PREREGISTERED 2（均为外部依赖：B-2 收割 / B-4 挂 GPU）/
  CANDIDATE 4（B-12 挂 GPU / argparse 白班项 / FwPKM-B10 文档更新 /
  档案漂移 L 待裁定）。**本地无未立案的可执行项 → 后续轮次按新规收工**，
  直至 ~11:04 B-2 收割窗口（优先插入）。

---

## 定时轮次（05:59 触发）：B-10 对照文档增补 — 扫描轮 3 文档化收尾

- **FwPKM-B10 更新项清账**：`docs/PMB_VS_2026_BENCHMARKS.md` 对照总览表新增
  Memora（2604.20006）/ FwPKM（2601.00671）两行 + 第六节增补（Memora 三差异
  化存活确认、FwPKM 归系统侧、mem0 行业最难问题与 t4 定位强化、同日 PMB 侧
  新证据索引）。commit `e30321a`。
- **池子终态（更新）**：PREREGISTERED 2（B-2 收割 / B-4 挂 GPU）/
  CANDIDATE 3（B-12 挂 GPU / argparse 白班 / 档案漂移 L 待裁定）。
  **本地池清空确认 → 后续轮次收工，直至 ~11:04 CST B-2 V14 收割窗口。**

---

## 收工轮（06:29）：池空，无动作

- 池子终态不变（PREREGISTERED 2 外部依赖 / CANDIDATE 3）；B-2 V14 仍
  RUNNING（已运行 ~7.4h/12h 上限，收割窗口 ~11:04 CST）。本轮按新规收工，
  无实验、无扫描（当日扫描配额已用讫）。

## 收工轮（06:58）：池空，无动作

- 状态不变：B-2 V14 RUNNING（~8.1h/12h），收割窗口 ~11:04 CST。按新规收工。

## 收工轮（07:29）：池空，无动作

- 状态不变：B-2 V14 RUNNING（~8.7h/12h），收割窗口 ~11:04 CST。按新规收工。

---

## 收工轮转扫描轮（07:29，用户质询触发）：扫描轮 4 — 睡眠巩固赛道命中

- **背景**：用户质询"没有主动联网搜新方向么"——事实核查：当日扫描轮 3
  （02:4x）已产出 B-14~B-19 且全部执行完；"当日至多一次扫描"上限是我设计
  的保守值，用户意图为"闲着就扫"→ **规则放宽为每轮轮换赛道扫描**（定时
  任务提示词已同步更新）。
- **本轮命中（巩固赛道）**：[SleepGate](https://arxiv.org/abs/2603.14517)
  （KV 记忆学习型睡眠周期）、[sleep replay 防灾难性遗忘](https://arxiv.org/abs/2606.08447)、
  [self-consolidation](https://arxiv.org/html/2606.03979v2) → **B-20 转正
  PREREGISTERED**：会话间巩固变换（downscale 阴性对照 + prune 稀疏化主变体）
  直接瞄准今晚发现的容量断崖——"睡眠窗口"与 PMB 会话边界钩子天然对接。
- **LNN 赛道**：无新进入者（基础架构无实质演进）。Hopfield/抗干扰赛道搜索
  超时，下轮轮换重试。
- B-20 预注册 commit `43d6a47`。下一轮：B-20 实现 + 判读（~分钟级）；
  收割窗口 ~11:04 CST 不变。

---

## 定时轮次（08:2x 触发）：扫描轮 5 + B-21 DEAD — 异关联迭代机制诊断

- **扫描轮 5（Hopfield/抗干扰赛道）**：2026 新论文均为物理实现/容量变体
  （[复值 Hopfield](https://globals.ieice.org/en_transactions/fundamentals/10.1587/transfun.2025MAP0008/_f)、
  [光子 dense AM](https://link.aps.org/doi/10.1103/7jh8-2vny)），无直接竞品；
  但经典谱系的**迭代吸引子读出**是读出侧最后一块的迁移检验 → B-21 转正
  并当轮执行完毕。
- **B-21 实现 + 结果**：read_iters/read_beta 入库（iter3 难格 **0.0**，
  iter2 s0 0.0——一次重进入即坍塌）。
- **机制诊断（可引用）**：**异关联记忆（key≠value 无关空间）不能直接套
  自关联 Hopfield 迭代**——首步读出落在值空间，重进入的键值重叠 ≈0
  （随机向量正交），信号首次重进入即归零。经典正解 = Kosko BAM 双向交替
  （F/Fᵀ）→ **B-22 CANDIDATE**（下轮执行，5 分钟）。
- **判据模板缺口（诚实记录）**：预注册未覆盖大负变化区间，-0.385 按意图
  判 DEAD；负变化显式入判据模板（与档案漂移 L 同批修订）。
- 证据 + 实现（commit `2876c79`）。
- **下一方向**：B-22（BAM 交替读出）→ B-2 收割（~11:04 CST）。

---

## 定时轮次（08:5x 触发）：B-22 DEAD — 读出侧终局加固

- **实现入库**：read_protocol="bam"（Kosko 双向交替，用经实证的读出映射对）；
  test 9/9 过。
- **结果**：BAM3 难格 **0.0**（3/3），BAM2 s0 亦 0.0——第一轮交替即坍塌；
  全格退化随干扰会话数加重。
- **判定 DEAD——读出侧终局加固**：朴素迭代（B-21）+ BAM 双向（B-22）两套
  经典读出修复全部无增益且有害。**三环节失败面终版**：存储可解（容量墙，
  B-17 d=4096→0.995）/ 巩固不可标量化（B-20 剂量曲线）/ 读出侧经典修复
  皆无增益（B-21/B-22）——**叠加态的部分信号（0.385）不可被读出补全，
  唯一有效轴是状态扩容**。
- **判据模板缺口第二次命中**（大负变化在字面区间外）→ 与档案漂移 L 合并的
  判据模板修订升格为 mechanism 小项。
- 证据 + 实现（commit `288db94`）。
- **下一方向**：B-2 V14 收割（~11:04 CST）优先；间隙 = 判据模板修订小项。

---

## 定时轮次（09:2x）：提示词增补社区赛道 + 扫描轮 6 首扫命中

- **提示词更新（用户指令）**：池空收工轮的扫描条款从"仅学术赛道"扩为
  **两类赛道轮换**——①学术（arXiv）；②**社区**（GitHub/HuggingFace/Reddit/
  HN/行业博客，商业价值档证据源）。社区线索同样必须过预注册判负标准。
- **社区赛道首扫命中**：LoCoMo 基准争议正烧——[Letta 文件系统 74.0% 胜
  Mem0 68.5%](https://www.letta.com/blog/benchmarking-ai-agent-memory/)、
  [MemGPT 作者"别信在线记忆基准"](https://www.linkedin.com/posts/charles-packer_dont-trust-everything-you-read-online-activity-7361113034754347008-Ic7L)、
  Label Studio 饱和论、r/LocalLLaMA "WWE feud"（[对比文](https://www.digitalapplied.com/blog/open-source-agent-memory-mem0-letta-zep-compared)、
  [生态数字](https://preuve.ai/blog/ai-memory-systems-statistics-2026)）。
  **社区最热痛点 = 记忆基准不可信/无区分度 = PMB 设计主张的靶场。**
- **B-23 转正 PREREGISTERED**：FilesystemSystem（纯文本落盘+grep 式检索，
  Letta 式平凡基线的 PMB 化）全任务 × 3 seeds 对 rag 同跑——社区争论的
  仲裁实验。判据：任务族胜负矩阵 + 预注册方向性预测（t5 幻答 100%、
  t4≈单跳 rag）。下轮实现+执行；B-10 文档补社区争论节。
- 生态数字（社区侧，引用需复核时点）：Mem0 ~41-62K stars/$24M、Graphiti
  ~28K、Letta ~24K。commit `e346904`。

---

## 定时轮次（09:3x 触发）：B-23 DONE — Filesystem 全任务族击穿，社区结论可信复现

- **实现入库**：`FilesystemSystem`（全文落盘 + overlap grep，无嵌入无索引，
  state=原始文本 O(history)）；test 9/9 过。
- **胜负矩阵（filesystem vs 今日同跑 rag，3-seed）**：t1 难格 **1.000 vs
  0.802** · t2 **1.000 vs 0.917** · t3 gap32 **1.000 vs 0.875** · **t4 JOIN
  难格 1.000 vs 0.083** · t5 known 1.0/幻答 1.0（方向性预测 ✓）。预注册判据
  (a) 多格触发 → **DONE**。
- **两个重磅注记**：① **t4"无解格"改判**——它对受限读出系统无解、对全量
  读出平凡可解（grep 无 JOIN 协议缺陷）；② **平凡基线反而小 13×**（同格
  177KB vs rag 2.27MB）且 recall 更高——"又小又强"。
- **学术结论（可引用）**：*"On PMB, a verbatim filesystem with grep solves
  every task family at smaller state than a vector-store retrieval layer —
  including the cross-session JOIN that defeats every memory system —
  reproducing the community's 'filesystem beats memory layers' finding under
  a program-generated, anti-enumeration harness. PMB arbitrates the debate."*
  ——Letta/Mem0 之争在可信 harness 下得到仲裁，且两者都对：特化层必须逐
  任务族向平凡基线自证。
- 证据 + 实现（commit `f9632d6`）。
- **下一方向**：judge 加 filesystem 行 + B-10 补社区争论节（mechanism 小项）；
  **~11:04 CST B-2 收割**优先。

---

## 定时轮次（09:5x 触发）：治理双清 — L009 立案 + 判据模板双侧化

- **L009 立案**（`kb/lessons/L009-same-run-cross-time-comparison.md`）：
  跨时点对照必须同跑——生成器演进使旧档案不可逐位复现（B-16 正则失配、
  B-19 修复前档案误引、B-6/B-8 漂移三实例入档）。
- **判据模板双侧化**（backlog 头部修订）：预注册判负标准必须写死
  ①正向阈值 ②**显式负向变化分支** ③中间带——B-21/B-22 两次大负变化落在
  判据字面外的缺口关闭。
- B-2 V14 仍 RUNNING（10:0x，~10.9h/12h）。commit `e8a3e3b`。
- **下一方向**：**B-2 收割**（~11:04 CST 截断后立即拉取+重推 V15）。

---

## 定时轮次（10:3x 触发）：扫描轮 7（社区）+ B-24 DONE — 多级算力路由器落地

- **用户指令**：调研社区多机算力实践（本地 1h 校验 / Kaggle 受限 / A100 级）
  并工程化落地。
- **扫描结论**：社区三主流 [SkyPilot](https://docs.skypilot.co/en/latest/sky-computing.html)
  （跨云套利）/ [dstack](https://news.ycombinator.com/item?id=42055036)
  （on-prem+云控制面）/ [Modal](https://modal.com/blog/serverless-gpu-article)
  （serverless 贵锁单云）；个人常见"训练 SkyPilot + 推理 Modal"。
  **三者均不覆盖 Kaggle 免费档** → 自建零依赖轻路由。
- **B-24 DONE**：`scripts/tiered_run.py`（T0 本机 ≤1h / T1 kaggle-cpu 12h /
  T2 kaggle-t4 9h 配额 / T3 a100 SSH 四层路由 + 各层 launch/harvest recipe +
  L010 检查内置——未钉死 OMP 的 launcher 拒绝 push）+
  `docs/COMPUTE_TIERS.md`。验证：四层 dry-run + L010 负例拦截 + T0 实跑 +
  T3 base64 引号安全。首个真实用例 = B-2 V15 推送。
- **台账乱序注记**：backlog 节顺序因锚点插入非线性（内容 grep 核对无丢失），
  重排登记白班项。commit `c68ace6`。
- **下一方向**：**B-2 收割**（截断 ~11:04 CST）+ 当轮推 V15。

---

## 收工轮（10:29）：池空，等收割窗口

- 状态：B-2 V14 RUNNING（~11.4h/12h，截断 ~11:04 CST）。下一轮进入
  **收割流程**：`kaggle kernels output` 拉取 → 入库验收 → 当轮推 V15
  （4 lane × OMP 钉死版已入库待推）。本轮按新规收工。

---

## 定时轮次（10:1x 触发）：V15 启动器入库 + L010 满核铁律（用户质询驱动）

- **V15 启动器入库**（`kaggle_kernels/tau_ladder_probe/run_tau_probe.py`）：
  从 Kaggle version 14 **精确拉取源码**重建——发现 **V14 的 launcher 从未
  入库**（档案缺口顺手关闭）；改动 = **OMP/MKL/OPENBLAS/NUMEXPR 环境钉死
  单线程**（父进程 set_num_threads 不传子进程，V14 实为 12 线程挤 4 核）+
  **4 lane** 跑 (depth,seed) 对队列（3/2/2/2，第 4 核不再闲置）。
  metadata 同步为 CPU（旧 PR 分支是 9/5 的 T4 版）。
- **L010 立案**（`kb/lessons/L010-kaggle-cpu-full-core.md`）：Kaggle CPU
  满核铁律——lane 数 = 核数、线程环境变量钉死必须在 spawn 之前、按配置
  原子落盘 resume-safe。会话窗数学：4 lane 预期 ~8.8 配置/会话（V14 3 lane
  ~6.5）。本地 A/B：OMP=4 快 24%（线程敏感实锤）。
- **预期**：V15 单会话 ~8.8 配置（若 OMP 超订确是主要拖累则可能一轮接近
  收官）；剂量响应 V15 日志可验证。commit `922d60c`。
- **下一方向**：**B-2 收割**（~11:04 CST）+ 当轮推 V15。

---

## 定时轮次（07:58 触发）：B-20 DEAD — 标量巩固判负，两则机制发现

- **实现入库**：`consolidate(mode, param)` 会话边界钩子（downscale/downscale_z/
  prune 三变体）+ CLI 透传 + 快照 nonzero_rate 诚实列；test_pmb 9/9 过。
- **结果**（param_sum @ d=256, 3 seeds）：prune 难格 **0.120**（lift -0.266，
  信号元与干扰元同量级，幅值剪枝无差别摧毁）；T3 gap=32 0.0。
- **阴性对照"爆表"追出真发现**：downscale α=0.9 难格 0.0——排查（α=1.0 恒等
  复现基线、剂量单调、随干扰会话数放大）确证非管线 bug 而是**机制**：
  **uniform 会话缩放 = 新近度加权 = 遗忘操作**。全局尺度在读出比值中消去，
  跨会话相对尺度不消去（早事实 α^16≈0.19 vs 末会话干扰 α^1≈0.9）。
  **结论：线性联想记忆 + 归一化读出下，标量巩固要么无操作、要么是遗忘
  日程——不存在"巩固"**。剂量曲线 α=0.99→0.36 / α=0.9→0.0 入档。
- **谱系闭环**：内容感知巩固 = 重放/重解 = micro_rls（B-19 审计已证其失败
  在读出侧）——巩固-存储-读出三环节失败面完整映射：存储可解（B-17）、
  巩固不可标量化（本轮）、读出侧未解。
- 证据：15 JSON + 实现（commit `815c7e4`）。
- **下一方向**：B-2 V14 收割（~11:04 CST）优先；间隙 = Hopfield 赛道补扫
  或收工。

---

## ⚠️ 重大复盘（10:5x）：B-2 V14 是 12 小时僵尸——真凶 = metadata 缺 dataset 挂载

- **事实**：按版本拉取日志发现，**V14（9/15 23:04 API 推送）在 4 秒内即崩溃**
  （`bundle not found: None`——API 推送的 metadata 缺 `dataset_sources`，
  bundle 数据集没挂载；kagglehub 私有集认证又不可用）。而 `kernels status`
  连续 12+ 小时报告 RUNNING（僵尸/队列假象）。**B-2 实际进度仍是 V12 的
  3/18，一夜零推进**。
- **我的 V15（10:4x）同因秒崩**：重写 metadata 时同样漏了 dataset_sources。
  且 v1 路由器 `run` 动作未把 --dry-run 传给 launch（回归 bug），导致本想
  dry-run 的测试真推了 V15——两个 bug 叠加反而把根因暴露了。
- **修复（已推 V16）**：metadata 补 `dataset_sources: [m1-b1-text-ab-bundle]`
  （挂载后 launcher 的兜底扫描就能找到 bundle，无需 kagglehub 认证）+
  tiered_run dry-run 回归修复。**V16 已 RUNNING 并活过 4 秒崩溃点**（10:57
  验证，生存 = 挂载修复生效的判别器）。
- **L011 立案**（待写文件）：`kernels status RUNNING ≠ 脚本存活`——推送后
  4 分钟内必须拉取启动日志验证，不能只信状态端点。
- **对今晚叙事的修正**：B-2 的"~10.5h 运行、~11:04 截断、速率 1.5 步/s"
  全部来自 V12 的历史实测，V14 一夜没有产出。预期更新：V16（4 lane × OMP
  钉死）12h 会话 ~8.8 配置，若 OMP 超订确是主要拖累则可能更多；18 配置齐
  预计 9/17。

## 收工轮（11:01）：V16 存活抽查通过

- B-2 V16 RUNNING（10:53 起跑，已越过 V14/V15 的 4 秒崩溃点 30+ 分钟——
  dataset 挂载修复生效，probe 训练中）。12h 窗至 ~22:53，按 L010 预期
  ~8.8 配置/会话。按新规收工，下轮起每 30 分钟按 L011 抽查存活。

## 收工轮（11:29）：V16 抽查正常

- B-2 V16 RUNNING（~36min/12h 窗）。按新规收工。

## 定时轮次（11:3x）：B-24 运营接线 — 工具成为正式操作路径

- 诚实盘点后的补课：B-24 此前只验证了层本身，B-2 的实际推送走的是裸 CLI。
  本轮完成**运营接线**：B-2 声明式作业规格入库
  （`kaggle_kernels/tau_ladder_probe/job.json`）+ tiered_run 修复三处
  （status/harvest 支持 --job、harvest_dest 入 spec、JSON 报错优雅化），
  真实 kernel 上回归全过（status 查到 B-2 RUNNING、harvest 目的地
  `benchmarks/results/tau_ladder/` 生效、L010 负例拦截 rc=2）。
- **定版**：今晚 ~22:53 V16 截断后的收割命令 =
  `python scripts/tiered_run.py harvest --job kaggle_kernels/tau_ladder_probe/job.json`。
  commit `ef60956`。

## 定时轮次（11:0x 触发）：checkpoint 契约落地 — 多主机训练状态汇聚

- **修复确认**：/tmp 测试规格缺 ckpt_dataset 字段是先前"降级未触发"的假象；
  补字段后全链路验证通过（403 缺失检测 → 剥离降级 → 引号安全 T3）。
- **B-24 增补（checkpoint 契约）**：状态 = 按配置原子 JSON；汇聚点 =
  Kaggle Dataset `m1-tau-ladder-ckpt`。harvest 推版本（DNS 故障仅警告）、
  launch 挂载 + 启动并入 → **跨会话/跨主机不重跑已完成配置**。
  边界：B-2 类是配置级并行（无需 torchrun）；梯度同步级走 T3 A100 fsdp。
- 文档 `docs/COMPUTE_TIERS.md` 六/七节；commit `b481d71`。
- **下一方向**：例行抽查 V16；**22:53 收割**（harvest 时以真实 JSON 重试
  checkpoint 建库）。

## 收工轮（12:20）：V16 抽查正常

- B-2 V16 RUNNING（~1.5h/12h 窗，至 ~22:53）。checkpoint 契约待收割时
  以真实 JSON 建库。按新规收工。

## 收工轮（12:28）：V16 抽查正常

- B-2 V16 RUNNING（~1.6h/12h 窗）。按新规收工。

## 收工轮（12:58）：V16 抽查正常

- B-2 V16 RUNNING（~2.1h/12h 窗）。按新规收工。

## PR 整备轮（22:3x，用户指令）：远程新分支合并 + PR #58 全面更新（不合 main）

- **远程新发现**：`evidence/tau-b2-flat-a100`（19:15 推送，另一会话/A100 通道）
  ——**B-2 已被完整执行并判负**：18/18 配置全饱和（parity d16 depth=1 即
  1.0），judge_verdict flat / delta_gain=0 / p=1.0；诚实标注"天花板效应，
  协议无法检验 H004"。合并入 overnight/loop（零冲突，validate 8/8 PASS）。
- **B-2 正式 DEAD 结案** + **B-2' 难度硬化重测立项 PREREGISTERED**（A100
  通道，判据同款）。Kaggle V16 判据冗余 → 令其 22:53 自然截断，重复产出
  不入库。
- **PR #58 更新**（REST API，原 GraphQL 被 Projects 弃用警告打断）：标题与
  描述改为全天循环总览（B-13'~B-24 + L009-L011 + B-2 结案）。**不合 main，
  待用户裁定。** 完整性核验：validate_results 8/8 PASS、无未推送提交、
  116 commits ahead of main。

## 分支整备轮（22:3x，用户指令）：全部分支推送确认 + 完善

- **盘点**：17 个本地分支全景核对——3 个有未推送提交，3 个纯本地档案，
  其余全部同步。
- **已推送**：evidence/arr-ratio-9arm（分支保护文档）；分叉两分支先合并
  远程再推（无冲突、无 force）：feat/tau-ladder-kernel-fix（ahead1 behind7
  → 合并推送，PR #53 内容补全）、govern/iteration-loop（同法，PR #52 内容
  补全）。本地 main 快进到 origin/main（daa3578→4338fd3，未 push main）。
- **终态**：全部有上游分支 ahead=0；待用户裁定项 = 3 个上游已删的档案
  分支（fix/archived-negative-modules、iter/latent-recursion、
  iter/o-series-hybrid-ratio——推则复活已删引用）+ 3 个未跟踪目录
  （.zcode、_rescue_20260831、c1_global_heads）。
- **B-2 V16**：22:38 仍 RUNNING，22:53 截断 → 本轮等待后收割。

## 收割轮（22:5x）：B-2 V16 收割 — 双硬件互证，tau-ladder 线关闭

- **分支整备已完成**（全部有上游分支 ahead=0；3 个档案分支待裁定）。
- **V16 截断收割**（tiered_run 首次真实全流程）：**7/18 配置完成**，
  mean_acc ≈ 1.0 —— **Kaggle 硬件独立复现 A100 的全饱和结论，flat 结案
  doubly confirmed**。**L010 剂量实证**：单配置 wall 4.70h（OMP 钉死）vs
  V14 同容器 5.5h，提速 ~15%。
- **事故与恢复**：harvest 同名覆盖了 A100 权威集 → git 恢复（L009 式警觉）；
  V16 复现集另立 `tau_ladder_kaggle_v16/` 谱系目录。checkpoint 数据集建库
  因本机 DNS 故障顺延（降级链生效，git 状态安全），下次 harvest 自动重试。
- **B-2 系列终态**：DEAD（flat，双硬件互证）/ B-2' 硬化重测 PREREGISTERED /
  V17 复跑 = 可选不做。**tau-ladder 线正式关闭。**
- 证据 commit `6f5859e`。
