# 晨报 2026-09-09（二航 / 第二班次）

> ⚠️ 仍是**手动触发**（16:28，非 23:00 定时窗口；同日两班）。本班关键实测结论：
> **headless 会话无法调用内建 /goal**（`Skill not found: goal`——/goal 是客户端 UI
> 命令，非 agent 可调用技能）→ **降级模式即实际运行模式**：验收句式自查 + 止损
> 规则 + 09:00 硬上限。建议（待用户裁定）：定时任务提示词里的"第一动作 /goal"段
> 改为降级自查协议为唯一路径，或测试在 prompt 里直接写 goal 句式能否触发客户端
> goal 挂载。

## 目标清单与达成情况（降级模式自查）

| # | 目标 | 状态 |
|---|---|---|
| 1 | ≥1 项 PREREGISTERED 推进至 DONE 或 DEAD | ✅ **B-6 DONE**（PMB 参照 9 JSON 入库，`e823214`） |
| 2 | ≥1 个 CANDIDATE 扫描后转正或判 DEAD | ✅ B-3 扫描完成 → 判"挂起受限"（GPU adapter 阻塞，非 DEAD）；B-4 获 LongMemEval 映射证据 |
| 3 | GPU 通道可用则推 1 kernel | — 前置不满足（#53 未合并 + Secret 未配），按协议记阻塞，不触发 |
| 4 | 未完成项晨报登记（=达成） | ✅ 见下 |
| 5 | 晨报产出并 commit+push | ✅ 本文件 |

**收工原因**：可执行目标全部达成。B-1 为跨夜长跑（健康运行中，断点已验证）；
池内剩余 PREREGISTERED（B-2 GPU 挂起、B-4 判负实验需 fastweight 臂）均有外部
依赖，继续空转违反"有效而非填满时间"。**提前收工。**

## 跑了什么

### B-6 · PMB 参照系统重跑（本班主产出）
- 命令：`PYTHONPATH=. .venv/bin/python benchmarks/persistent_memory/run_pmb.py
  --task all --system {none,rag,oracle} --seed {0,1,2} --out_json benchmarks/results/pmb/pmb_{sys}_s{seed}.json`
- 单配置 ~0.5s CPU，9 配置全网格一次跑完；
- 验收门：`scripts/validate_results.py` **PASS**（8 passed）；
- 产物：`benchmarks/results/pmb/` 9 个 JSON（pmb-v0 协议，含 config/网格/stale/延迟全列）。

### B-1 · text_selective 32K（跨夜长跑，运行中）
- 预注册 commit `bc5ac38`（首航）；本班检查点：step 4800、loss 5.49、
  **首个中途断点 sel0_s0.pt 已落盘**（首航加的 2000 步存档机制生效实证）；
- 进程 92378（pid 文件 notes/overnight/runs/b1_32k.pid），log 完整；
- 判读预计第 3 夜完成 6 run 后（`analyze_text_ab.py` 按 steps 自动分组）。

## 过了哪道验收门

validate_results.py（B-6 产物）；py_compile（无新脚本改动）；PMB 输出结构
人工核验（oracle 0.0 复核为设计性超窗塌陷，非 bug——systems.py 注释与 README
双重印证）。

## 结果 JSON 路径

- `benchmarks/results/pmb/pmb_{none,rag,oracle}_s{0,1,2}.json`（9 文件，本班新入库）

## 本班 commit 与 PR

- `e823214` evidence(pmb): 参照系统重跑入库（B-6 DONE + backlog 修正）
- 常驻 PR **#54** 自动累积（现 3 commits）；PR #50-53 状态均未变（用户未处理）
- backlog 统计：PREREGISTERED 2 / CANDIDATE 2 / **DONE 1** / DEAD 0

## 异常与已落工件

- **L001 实例 ×2**（同一夜班两次命中同一条教训，按 §4.2 应记录）：
  ① 首航把 B-4 登记为"PMN 加 D5 遗忘曲线"，实际 PMB 已有 T3 遗忘曲线任务族
  （登记前未盘点内部现状）；② "rag/hash 0.78" 二手数字无落盘（本次重测 0.839
  才有一手数据）。已按纪律在 backlog 修正并清偿；**是否升格为新 L 工件
  （"方向登记前必须先盘点内部既有资产"）→ 晨报待判定项**。
- /goal headless 不可用 → 非异常，是设计边界确认（记入晨报结论）。
- 无止损触发，无 ADJ 草案。

## GPU 通道状态

**仍阻塞**（#53 OPEN + Kaggle Secrets 未配）。本班配额消耗 0h。
B-2（tau 分片）与 B-3 fastweight 臂继续挂起。

## 明晚建议

1. 若 #53/#52 已合并 + Secret 已配：**B-2 tau 分片 1**（GPU 夜）+ B-1 断点接续；
2. 若仍阻塞：B-4 判负实验的 fastweight 臂同样被阻塞，可选轮次 =
   ① LongMemEval MSR/Abstention 任务族的 t4/t5 定义扫描（纯 CPU 扫描轮）；
   ② E3b 判别点核算（纯分析，ITERATION_PRINCIPLES §3）；
3. B-1 继续（预计第 3 夜出 6 run 全数据 → analyze 判读）。

## 需 AricRedemption 人工判定

- [ ] **#53 合并 + Kaggle Secrets `GITHUB_TOKEN`**（GPU 通道，最高优先）
- [ ] **#52 合并**（治理护栏）；**#51 关闭**（空壳）
- [ ] **#54**：立即合或攒批按 ADJ-009 窗口合（现 3 commits，含 PMB 参照 evidence）
- [ ] HANDOFF 的 "0.78" 表述是否按重测 0.839 订正（原出处不可考）
- [ ] L001 两连击是否升格新 L 工件（"登记前先盘点内部既有资产"）
- [ ] 定时任务 /goal 段是否改为降级自查协议为唯一路径（headless 不可用已实测）

---

## 夜班增量（2026-09-09 23:00 定时派发，run 3/11）

- 开场恢复：merge origin/main（#50 已合并 + #55 falcon 判负证据已入）；B-1 存活 6h52m（run 3/6，step 5200+）
- /goal headless 不可用已实证（Skill not found）→ 降级模式即正式运行模式
- **B-7 正式 DEAD**：白班 s0 screening → 夜班补齐 s1/s2 + sum 对照 → 3-seed [0.0,0,0] 均值 0.000 ≤ 0.739；证据 6 JSON 入 pmb_falcon/，commit 796a680
- 撞车协调：白班与夜班同方向并行执行，rebase 冲突已合并解决（backlog 单一条目双证据）
- 让路条款生效：系统总占用 506%>50%（B-1 360% + 其他），新训练降级为扫描/分析轮
- GPU 通道：#53 仍 OPEN，Secret 未配 → 维持阻塞
- 遗留欠账：B-1 剩 ~3 run（明晚判读）；B-2/B-3 挂起等 GPU；B-4 判负实验（fastweight 臂）等 GPU
- 待人工判定：#53 合并 + Secret 配置（GPU 关键路径）；#52 合并；#51 关闭；#54/#55 处置

---

## 夜班续（2026-09-10 09:4x）：B-1 冗余乌龙自曝

- **乌龙**：夜班 23:00 开场未发现白班已在服务器 A100 判负 B-1（c9112cc2，13:58 提交），
  误将 V8 的 CANCEL_ACKNOWLEDGED（16:33 人为裁定取消）当作 12h 超时，
  推 v9/v10/v11 三个 kernel 重跑已 DEAD 方向 —— 纯无用功。
- **根因（L 工件候选）**：开场恢复协议只检查"origin/main 有新合入则 merge"，
  **没有检查工作分支自身（origin/overnight/loop）的远程更新**——白班的
  B-1 DEAD 登记就在这个分支上，开场 fetch 后未 pull 未读。
- **处置**：v11 已用空脚本覆盖终止（version 12）；本地已 pull 同步白班全部记录。
- **B-1 终态确认**：DEAD（A100 3-seed：stock 387.10±3.49 vs selective 386.15±4.10，
  2/3 胜但 p=0.5 → 预注册 p 值条件触发；白班边界注：字面"胜≤1/3"未满足，诚实标注）。
- **代价**：3 次 kernel push + ~2h 无用 CPU（Kaggle CPU 免配额）+ 夜班注意力。
  换来一条教训：**多会话共写同一分支时，开场必须 pull 工作分支自身**。

---

## 循环轮次（2026-09-11 上午）：B-8 DONE + #54 合并

- **#54 已 squash 合并进 main**（9228ae0）：复核 CI 3/3 绿、红线干净、validate/check_claims PASS、内容抽查通过；用户授权合并。已知缺口记录：B-1 selective 侧 val_ppl 在 backlog 文本级，A100 结果 JSON 待白班补落。
- **B-8 DONE**：PMB 扩 t4/t5 任务族。参照数：t4 上检索式与固定窗口双失败（rag 难格 0.0、oracle k=4 塌陷）、t5 上 rag 幻答率 100%——两个结构性失败模式暴露，fastweight 假设检验位置登记（待 GPU）。
- **预注册裁定注记**：t4 的 rag-oracle 字面差距条件在 (4,0) 格触发，解读与是否回炉 → 待用户裁定。
- 新方向候选（下轮浮现）：rag --encoder e5 对照（编码器敏感度，工程/学术混合）；"JOIN+拒答双失败"的学术叙事写作（PAPER_SKELETON 素材）。

---

## 循环轮次（2026-09-11 09:4x）：B-9 DONE — 编码器敏感度 3-seed 判定

- **(a) 达标**：t1 难格 e5 = 1.0（hash 0.839）——语义编码器不伤精确检索反而更好
- **(b) 达标**：t4 JOIN e5 难格 0.5 / (4,4) 0.667（hash 0.0）——B-8"JOIN 双失败"
  修正为"hash 检索盲区为主"，语义编码器救回 50-75%
- **(c) 发现**：t5 幻答率 e5 = hash = 1.0——**拒答失败与编码器无关**，是 evidence
  模式结构性极限；t5b（abstention 决策层协议）浮现为新方向
- 证据：benchmarks/results/pmb_e5/（3 JSON）+ commit 7603a13
- 遗留：B-2/B-3/B-4 判负实验仍挂 GPU 通道；t5b 待预注册

---

## 验证 3（2026-09-11 上午）：fastweight-on-MPS 可行性探针 ✅

- **探针设计**：微型 MT-LNN（d_model=104/2 layers，gwtb_n_heads=1）在 MPS 上
  走完 前向→反向→10 步 AdamW 训练循环。
- **结果**：eval 前向 OK（loss 5.53）、训练步 OK、10 步 loss 单调下降
  （5.59→5.18）、吞吐 18.7 步/s（batch=2/seq=64 探针级）。
- **两个坑**（已记录）：① gwtb d_gw 整除约束（gwtb_n_heads=4 与 d_gw=13 冲突，
  改 n_heads=1）；② MPS sdpa 不支持 dropout 分支——即使 p=0.0，
  model.train() 下 scaled_dot_product_attention 抛 NotImplementedError，
  但实测 train() + AdamW 正常跑（该报错出现于另一调用路径，eval 模式稳定）。
- **结论**：**B-4 的 fastweight 臂可以在本机 MPS 上做可行性验证**
  （小 adapter 训练 + PMB 推理），T4 只在需要更大规模时才必要。
  GPU 通道阻塞对 B-4 的"硬阻塞"降级为"规模上限受限"。

---

## 验证批次（2026-09-11 上午续）：B-4' 微型梯度写 DEAD 落地

- MicroFastWeightSystem 实现（线性联结记忆 d=104 + 每事实 8 步 SGD 梯度写，
  经典 fast-weight 灾难性干扰原型）；t1/t4/t5 全网格 × 3 seeds。
- **判负落地**：t1 难格 0.0（(4,0) 仅 0.167——灾难性干扰直接观测）、
  t4 JOIN 0.0、t5 known 0.083/幻答 1.0。
- **科学结论升级**：跨会话持久化失败的写规则覆盖面现在完整——闭式
  （sum/delta/falcon_nlms）与梯度（SGD）**全灭**；检索式 rag 0.839 / e5 1.0
  唯一有效。主轴 (a) 的 fastweight 定位由此坐实：缺口在持久化机制，
  不在任何写规则。
- 9 JSON 入库，commit 8892f40。

---

## 验证批次（2026-09-11 上午续 2）：B-4 fastweight 臂本机冒烟 ✅ 部分通

- **链路全通**：TinyLlama-1.1B-Chat 下载（10.6min）→ v2 adapter + LoRA(r=8)
  权重加载（unexpected 0）→ FastWeightSystem 实例化 → t1/t5 单 seed 冒烟跑完
  （1.2min 总）。
- **冒烟数字**：t1 recall 0.0（4 题，answer_latency 292ms/问）；
  t5 known 0.0 / **幻答 0.0**（有趣：SFT 后的模型生成无法稳定输出 6 位码格式
  —— 生成 64 token 里没有匹配的 gold 码串，非格式即内容失败）；
  **state_bytes = 51.4MB/会话**（adapter 流 + sqlite store 的全量持久化，
  对照 rag 的 ~13KB——"bit 级持久化代价"的第一个实测数字）。
- **调试记录**（3 个坑）：① attach 用 v1 参数但 checkpoint 是 v2s → 改用
  attach_mt_v2_adapters；② LoRA 键名 `.default.` 前缀的 PEFT 版本差异 →
  键名兼容映射；③ systems.py 的 `_sent_enc.dim()` 属性误写为方法 → 修复。
- **419 MB/4 会话 checkpoint 里 missing 201** = TinyLlama 本体（正常）。
- **待判定问题**（s0 数字太小不下结论）：fastweight 召回 0.0 是"SFT adapter
  没学会事实绑定"（预期内——它没在 PMB 任务上训过）还是"机制性失败"——
  需要在 PMB 任务上做小型 adapter 微调（这正是 E2 训练交付物）才能区分。
- **冒烟级结论**：链路 ✅，数字为机制参考非判定依据（单 seed + 未适配）。

---

## 夜班 23:00 班次（run 3/11 正式开场）：B-13 补全 + 扫描轮

- 开场 merge origin/main（#54 证据链已入 main ✅ + #57 2B 骨架 + #59 mirror sync）
- param(sum) t1 补跑：**难格 0.385 → 参数化最优易主回 sum**（delta 0.24 被超越）
- 扫描轮：B-12 MoNe 无公开代码降优先；新发现 MemoryBench（arXiv:2510.17281）
  归入 B-10 对照范围（三差异化判定不变）
- **B-2 tau：V7 运行中（18:52 起，跨夜）**——明晨出 18 配置判定数据

---

## 验证批次收尾（2026-09-12 00:4x-01:3x）

### 验证 A 补充：e5 阈值叠加 3-seed ✅
- e5 阈值 0.84：3-seed 幻答全 0.0，known recall 全 1.0（完美分离）
- **hash 阈值 0.5 的 trade-off 发现**：t1 难格 (64,16) 0.78→0.64（伤精确检索 -14pt）
  —— e5 语义分数对 known/unknown 的分离优于 hash 词袋（已知事实）
- **t5b 完整结论**：阈值拒答有效但**需语义编码器**，hash 词袋分数不可分

### 验证 B：B-11 MCB 映射结案
- MCB 四分法（persist/use/verify/ask）与 PMB 正交互补：
  PMB t5 拒答是 MCB ask 决策的机械基底
- B-11 不需要独立实验（映射结案，归档）

### 池子终态（扫描轮 3 完成，真穷尽确认）
- **DONE 5**：B-6/B-8/B-9/B-10/B-13
- **DEAD 3**：B-1/B-7/B-4'
- **映射结案 1**：B-11
- **PREREGISTERED 2**：B-2 RUNNING（Kaggle）/ B-4（挂 GPU+adapter）
- **CANDIDATE 3**：B-12（MoNe 挂 GPU）/ t5b-e5 叠加 DONE 升格 / 工程债 argparse
- **GPU 通道阻塞不变**（#53+Secret）——剩余方向全部卡此

---

## 夜班 23:00 班次（9/15）：B-2 tau CPU 续跑

- **开场恢复**：overnight/loop 已同步 origin/main（#59 mirror sync）
- **B-2 tau 状态确认**：V12 被 12h 上限截断，3/18 配置完成（全 OFF 对照臂，acc≈1.0）
- **处置**：V14 重推（resume-safe 跳过已完成的 3 个），继续剩余 15 个配置
- **速率实测**：~1.5 步/s（CPU notebook），单配置 ~5.5h，18 配置需 ~3 个 12h 会话
- **CPU notebook 不烧 GPU 配额** → 可跨多天慢慢推进

---

## 全量验证批次（2026-09-12 凌晨-上午）：五项验证全部完成

### A: t5b-e5 叠加 ✅
- e5 阈值 0.84：3-seed 幻答全 0.0，known recall 全 1.0（完美分离）
- hash 阈值 0.5：t1 难格 0.78→0.64（伤精确检索 -14pt）→ **e5 语义分离优于 hash**
- 结论：阈值拒答有效但需语义编码器

### B: ReplayFW 交错回放（B-4' 扩展）✅
- t1(64,16)：0.016-0.031（vs 无回放 0.0，微幅改善但不显著）
- t4 JOIN：0.0（交错回放无法修复 JOIN，两步查询超出线性联结表达范围）
- 结论：灾难性干扰的文献解法（交错回放）在微型模型上效果有限

### C: t2 × micro_fw/micro_rls 补全 ✅
- micro_fw t2 recall 0.792 / stale 0.0
- micro_rls t2 recall 0.0 / stale 0.042
- 矩阵最终态：5 任务族 × 7 系统全网格

### D: B-11 MCB 映射结案 ✅
- MCB 四分法与 PMB 正交互补
- t5 拒答是 MCB ask 决策的机械基底

### E: README 五轴 + Related work ✅
- 三轴扩展为五轴 + Related work 节（95d1898）

### 真穷尽确认

连续 3 轮扫描无新可执行 CANDIDATE。剩余方向全部需 GPU 通道或用户裁定。
循环到了真穷尽点——本机可做方向已全部跑完。
